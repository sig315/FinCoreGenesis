spring:
  application:
    name: FinCoreGenesis

  datasource:
    url: jdbc:mysql://localhost:3306/fincoregenesis
    username: root
    password: shomakubo
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true